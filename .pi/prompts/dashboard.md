---
description: Show the Equaxis runtime dashboard
argument-hint: "[--json]"
---
Run the Equaxis runtime dashboard now and show me the result.

Use this command from the repository root:

```bash
node scripts/equaxis.mjs runtime dashboard ${ARGUMENTS:-}
```

If the command fails, report the exact error and suggest the next diagnostic command. Do not start a normal Equaxis/Pi session for this request.
